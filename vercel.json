{
  "buildCommand": "rm -rf .vercel-out && mkdir -p .vercel-out && cp ai_infra_financing_report.html .vercel-out/index.html && cp favicon.ico agent.json llms.txt .vercel-out/ && cp -R public/. .vercel-out/",
  "outputDirectory": ".vercel-out",
  "cleanUrls": true
}
